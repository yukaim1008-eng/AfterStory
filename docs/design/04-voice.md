# 语音与音频

状态：尚未接入。领域边界与失败行为已确认；用户明确等聊天效果获其认可后再推进语音，当前不选择供应商或实现接口。

## 职责与已确认约束

遵循基线第 4、5、7 节及 5.2 第 12 项。接收回复中需朗读的信息和 Voice Profile；文字先形成，TTS 失败保留文字，重试不重复更新关系和记忆。

前端默认手动播放的偏好已确认，可提前保存偏好，但不代表存在可播放音频、TTS 或 Voice Profile。整体流程跑通不是开始语音的触发条件，需用户认可聊天效果。

## 待设计

- Provider、Voice Profile、音频状态和重试契约。
- 供应商、模型、声音素材、情绪参数映射与失败策略。
- 本地音频存储、访问隔离、生命周期和清理。

## 验收方向

验证文字与声音对应、失败降级、重试幂等及不跨角色串音。
