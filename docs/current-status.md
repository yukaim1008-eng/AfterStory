# AfterStory 当前项目状态

- 最近更新：2026-09-10
- 当前阶段：M1 后端文字链路工程验证完成，待真实模型验证
- 代码状态：已实现 FastAPI、PostgreSQL 迁移、角色导入、会话持久化、DeepSeek 适配器与重试隔离逻辑

## 已完成

- 模型配置已改为命名模型组：每组独立绑定供应商、API 地址、密钥、model 和参数，`LLM_ACTIVE_MODEL` 选择当前组。15 项测试通过，支持多个供应商配置共存；当前仍使用一个激活模型。

- M1：真实 PostgreSQL 上 12 项自动测试通过；实际后端进程启动、两个测试角色共八轮对话与重启恢复验证通过（fake Provider）。Ruff 与 Alembic 一致性检查通过。
- 已提供 `docs/running-m1.md` 运行说明、`uv.lock` 依赖锁文件和 `scripts/smoke.py` 进程验证脚本。
- 本地旧 SQLite 配置已修正为 PostgreSQL，空白模型配置已补为 DeepSeek；未写入或输出密钥。

- 已创建 AfterStory Git 仓库，当前使用 `main` 分支。
- 已确定产品以长期角色陪伴为核心，个人助手能力作为后续扩展方向。
- 已确定采用“跨世界陪伴”作为默认交互前提：角色保留原作身份并知道用户来自另一个世界，但不认为自己是 AI。
- 已确定原作经历属于 Character Definition，角色与用户通过 AfterStory 形成的新经历属于对应 Character Instance。
- 已确定 V1 主角色为《异环》的娜娜莉，当前用户在原作关系中映射为游戏里的“鉴定师”。
- 已确定娜娜莉与用户并非跨世界陌生人，双方承接原作中共同冒险、并肩行动的伙伴关系，再通过 AfterStory 继续发展。
- 已确定每个精选角色必须存在最低 Canon 基线；不同用户的 Character Instance 可以停留在不同 Canon Checkpoint。
- 已确定原作更新不能直接覆盖旧内容；系统可以提示新版本，但由用户确认后手动更新，避免剧透和时间线错乱。
- 已确定 V1 先完成一个完整主角色和少量测试角色，暂不开发用户角色创建器。
- 已确定使用 `character_id` 标识角色身份，名称仅用于展示并允许重名。
- 已确定精选角色和未来的自定义角色使用同一套 Character Definition 规则。
- 已确定通过 Character Instance 隔离不同用户、角色和关系实例的 Memory、State、Relationship 与 Conversation。
- 已确定 V1 的三个核心领域为 Character System、Conversation Core 和 Voice Layer，三者需要保持清晰边界。
- 已确定前端使用 Vue 3、TypeScript 和 Vite，后端使用 Python 和 FastAPI，数据库使用 PostgreSQL。
- 已确定 V1 使用普通 Python 工作流，不提前引入 LangGraph。
- 已确定先完成并验收后端核心链路，再进行前端页面结构、交互原型、视觉设计和 Vue 实现。
- 已将产品和架构决策整理到 `docs/decisions/0001-v1-baseline.md`。

- 已确定角色成长边界：核心身份和性格保持稳定，对用户的了解、信任、称呼及亲近表达可以随具体互动逐渐变化。
- 已确定角色定义、实例、短期状态、长期关系与个人记忆的职责边界，详见决策基线 5.1 节。
- 已确定关系不能简化为不断上涨的好感度，也不能仅凭聊天轮数自动进入恋人关系。
- 已确定用户只能通过角色的表达和对话中的行为感受关系变化，不展示好感度、关系进度条或关系升级提示。
- 已整批确认十二项相处、记忆与回复规则，详见决策基线 5.2 节：内部关系采用熟悉、信任、亲近三个方面；保留角色自主立场；恋爱不自动升级；缺席不自动惩罚；记忆有选择、可纠正、可管理；不模拟完整后台生活；主动关心限于对话内；情绪跨会话延续并可缓和；语音失败不影响文字且重试不重复写入关系和记忆。

## 正在讨论

- M1 范围已确认：原创测试角色、后端接口与完整文字回复、本地开发身份、Docker PostgreSQL；首次真实文本模型为 DeepSeek V4 Flash，后续比较表达效果。详见 `docs/design/07-first-text-milestone.md`。
- 已核实 DeepSeek 官方模型标识为 `deepseek-v4-flash`；Docker 和 PostgreSQL 已运行并完成验证。本地密钥尚未填写，真实模型调用与表达效果待验收。

- 已确定采用分区文档、整批决策、分阶段实施的工作方式，见 `AGENTS.md`；分区入口见 `docs/design/README.md`。
- 理解与回复的详细意图识别、说话规则暂缓定稿，上一批十二项理解机制建议未被整批采纳。
- M1 接口与持久化已实现；当前阶段只使用角色定义及最近成功对话，未提前实现意图分类或长期记忆算法。

- 娜娜莉最低 Canon 基线采用 1.1、1.2，还是某个更准确的主线剧情节点。
- 娜娜莉与鉴定师伙伴关系对应的具体初始状态和关系强度。
- Character Definition、Character State、Relationship、Character Response 和 Memory 的具体字段及实现细节；产品行为规则已确认，不再重复讨论。
- 主角色资料、行为方式和 Voice Profile 如何分别组织并保持隔离。

## 下一步

1. 在本地 `.env` 填入 `DEEPSEEK_API_KEY` 后执行 DeepSeek 真实多轮验证，记录模型连通性与角色表达效果；不把 fake 测试结果当作真实模型验收。
2. 根据娜娜莉和少量差异化测试角色，验证角色规则是否通用。
3. 将娜娜莉具体 Canon 基线、角色资料和数据来源作为独立阶段处理，不在当前讨论中提前展开。
4. 后续按模块确定 Embedding 和 TTS 的供应商与基本边界；首个 LLM 已确定。
5. 在 M1 上逐区深化记忆、状态、关系和理解机制，整批讨论产品取舍并按模块验收。
6. 后端核心链路完成并验收后，再开始前端设计与开发。

## V1 明确暂不进行

- 用户角色创建器。
- 天气、日历等工具调用。
- LangGraph 工作流。
- 微信接入。
- ASR、VAD、实时语音输入和全双工语音。
- 角色社区、角色市场和大规模商业运营能力。

## 后续方向

- V1 跑通后增加最简单的只读工具调用，再根据条件分支、确认、重试等需要评估 LangGraph。
- 后续开发用户角色创建器，但其产物必须使用与精选角色相同的 Character Definition 规则。
- 微信接入阶段优先考虑把 OpenClaw 和腾讯微信渠道插件作为独立消息网关，Character Runtime 保持渠道无关。
- 前端正式开发前先单独完成页面原型与视觉规范，避免在功能开发过程中不断堆叠临时界面。

## 当前待同步状态

- `README.md`、`docs/current-status.md` 和 `docs/decisions/0001-v1-baseline.md` 当前仅存在于本地工作区，尚未提交。
- 当前仓库尚未配置 Git 远程地址，因此另一台设备暂时无法通过 Git 获取这些新增内容。
- `.env.example`、`compose.yaml` 与 M1 代码已写入本地工作区；PostgreSQL 已启动、迁移并验证，当前阶段修改尚未提交。
