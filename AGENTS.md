# 项目协作规则

## 用户工作方式

- 从零开始项目时，先确定整体方向，再划分模块，为每个模块建立独立设计文档，逐区讨论、设计与实现。
- 不将整个项目的所有细节堆在一份文档或一条长对话中，也不未经分区设计就一次性生成全部实现。
- 相关的产品取舍整理成一批，附建议和影响，供用户一次判断；不要逐条反复确认。
- 已确认决策直接沿用；普通可逆实现细节由 Agent 自行处理，不将每个技术细节都交给用户审批。
- 明确区分已确认规则、建议方案、待定问题与暂缓内容。用户表示可以先搭框架，不等于采纳此前提出的全部详细算法和行为规则。
- 实施按有明确范围、输入输出和验收条件的阶段推进。Agent 可以在已授权阶段内持续实现、验证和修复，不自行扩展成整个项目的一次性开发。
- 当用户明确批准一个包含多个阶段的完整方案并要求连续执行时，该方案整体视为已选定范围。Agent 按顺序完成各阶段；每个阶段均执行实现、检查、测试、复查与必要修复，确认达到验收条件后单独提交，并立即进入下一阶段，不在阶段之间停下等待用户催促或重复确认。全部阶段完成后统一汇总结果与提交记录。提交用中文命名
- 每个阶段完成后同步模块文档和当前状态，使后续独立任务可以从文档恢复上下文。跨模块修改要同步接口及受影响文档。
- 测试结束后，Agent 必须停止自己为本次测试启动的前端、后端及项目依赖服务，不为方便预览而继续后台运行；用户需要查看效果时会自行启动。仅停止可确认由 Agent 启动的进程或容器，不关闭用户自行启动的服务，不删除持久化数据。用户明确要求保持运行时除外。

* 未明确授权连续执行多阶段完整方案时，每轮只推进已选定的一个阶段。
* 文档整理完成就立即结束，不继续顺带开发。
* 超过一分钟的操作会明确说明正在等什么。
* 测试完成后关闭我启动的服务。
* 不再把大量图片生成、依赖下载、真实联调和整套实现塞进同一轮。
* 达到本轮验收条件就停止，不反复追加“顺便优化”。

## 文档与执行入口

- 开始工作先读 `docs/current-status.md`、`docs/design/README.md` 和本次涉及的模块文档。
- `docs/decisions/0001-v1-baseline.md` 保存已确认产品与技术基线；模块文档引用它，不复制出相互冲突的规则。
- 本项目三个核心领域仍为 Character System、Conversation Core、Voice Layer。设计文档的进一步分区不意味着拆成独立服务。
- 遵守既有 V1 范围；前端顺序按下方用户调整执行，前端跑通不等同于完整 V1 验收。
- 2026-09-10 用户明确调整：已预留 `frontend/`，允许在独立窗口提前开展页面、交互和视觉设计；业务实现仍在设计完成且所需后端能力明确后推进。设计上下文见 `frontend/README.md`。
- 2026-09-11 用户调整前端设计节奏：在已确认整体方向后，一次交付全部桌面页面及必要状态供集中评审，用户指出不满意处后统一修改；不再每个页面或状态逐次停下确认。仍分区维护文档；当时业务开发需用户明确同意，现按下一条授权推进。
- 2026-09-12 用户认可设计并授权前端开发：先做可运行页面与现有文字链路，再收集资料、优化效果。性格、主题、素材、个性化按钮为可更改数据，不写死；记忆、语音和 Canon 更新仍分阶段推进。
- F1 流程跑通后的用户调整：当前先整理文档和已确认的基础规则，由用户决定下一阶段；暂不调整性格或建设角色资料库，语音等用户认可聊天效果后再做。不得将此前 Agent 的优先级建议作为已确认开发顺序。
- 这些规则记录用户可复用的协作偏好，但本文件仅在本仓库生效；不得声称已自动应用到其他新项目。
